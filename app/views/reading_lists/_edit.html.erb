<%= semantic_form_for @reading_list do |f| %>
    <%= f.inputs do %>
        <%= f.input :works, as: :check_boxes, member_label: :full_title, collection: Work.all %>
    <% end %>
    <%= f.actions %>
<% end %>
