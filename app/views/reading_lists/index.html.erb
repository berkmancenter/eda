<ul>
    <% @reading_lists.each do |reading_list| %>
        <li><%= link_to reading_list.name, reading_list_path(reading_list) %></li>
    <% end %>
</ul>
<%= form_for ReadingList.new do |f| %>
    <%= f.text_field :name %>
    <%= f.submit 'New Reading List' %>
<% end %>
