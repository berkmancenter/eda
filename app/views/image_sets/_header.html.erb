<header>
    <h2 class="edition-name"><%= edition.name %></h2>
    <span class="edition-creator">
        &mdash; <%= user_signed_in? && edition.owner == current_user ? 'Me' : edition.author %>
    </span>
    <h3 class="image-set-name"><%= image_set.image.metadata['Label'] %></h3>
</header>
