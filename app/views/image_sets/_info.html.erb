<div id="image-set-info">
  <% unless image.metadata.nil? %>
  <dl class="credits">
      <dt class="image-credits">Credits</dt><dd><%= image.credits %></dd>
      <%= render partial: 'shared/metadata', locals: { object: image } %>
  </dl>
  <% end %>
</div>
