<h1>Work#index</h1>
<ul>
<% @works.each do |work| %>
    <li><%= link_to(raw("#{@edition.work_number_prefix}#{work.number}#{work.variant}. #{work.title}"), edition_work_path(@edition, work)) %></li>
<% end %>
</ul>

