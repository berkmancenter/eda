<%= semantic_form_for [edition, work] do |f| %>
    <%= f.input :number %>
    <%= f.input :variant %>
    <%= f.input :title %>
    <% if work.new? %>
        <%= text_area_tag 'work[text]', size: "60x30" %>
    <% else %>
        <%= text_area_tag 'work[text]', flat_text(work), size: "60x#{lines_in_text_area(work)}" %>
    <% end %>
    <%= f.action :submit %>
<% end %>
