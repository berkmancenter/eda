<ol class="sortable_tree" data-rebuild_url="<%= rebuild_edition_image_groups_url(@edition) %>">
<%= build_server_tree(@image_groups, type: :sortable, edition: @edition, title: :name, rebuild_url: rebuild_edition_image_groups_url(@edition)) %>
</ol>
